## 2026-05-24 - [Prefetching and TanStack Query Integration]
**Learning:** Moving from manual `fetch` calls in `useEffect` to TanStack Query hooks reduces code complexity and enables advanced performance patterns like hover-based prefetching.
**Action:** Always check if a data-fetching component can be migrated to TanStack Query to leverage its caching and prefetching capabilities for better perceived performance.
